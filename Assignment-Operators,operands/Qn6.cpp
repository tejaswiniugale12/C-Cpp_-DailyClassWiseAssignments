#include <iostream>
using namespace std;

int main()
{
	char c;
	cout << " enter the alphabet or numeric or any other character " << endl;
	cin >> c;
	cout << "entered character is " << c << " and the Equivalent ASCII value is " << int(c);
	return 0;
}